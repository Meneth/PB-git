name = "Project Balance" # name of your mod, shown in launcher
path = "mod/ProjectBalance" # location of your mod files 
#replace_path = "history/provinces" # optional. if you want to repalce a path instead of just extend the files already in the base game (liek if you want to have completely new technologies)
user_dir = "ProjectBalance" # optional. creates a sub directory where settings/map cache/savegames etc can be safely written for your mod and not get mixed up 
dependencies = { "-ARKOpack Armoiries-" }